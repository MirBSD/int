{
  "jobs": {
    "vm-fbsd": {
      "runs-on": "macos-12",
      "steps": [
        {
          "uses": "actions/checkout@v3.5.2"
        },
        {
          "run": "(git gc || :)"
        },
        {
          "uses": "vmactions/freebsd-vm@v0.3.0",
          "with": {
            "copyback": false,
            "mem": 2048,
            "run": ": \"${CC=cc}${CFLAGS=-O2}\"; export LDFLAGS; exec sh mkt-int.sh $CC $CPPFLAGS $CFLAGS -Wall -Wextra -Wformat",
            "usesh": true
          }
        }
      ]
    }
  },
  "name": "vm-fbsd",
  "on": {
    "workflow_dispatch": null
  }
}
